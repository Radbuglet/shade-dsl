const WHEE = {
    (a, b, c) * d;

    if !whee {
        todo["whee", 'w'];
        foo("kk", {
            foo
        }).push(("hihi"))[haha]
    } else {
        whee;
    }

    {
        foo
    }
    a + b
};
