const WHEE = {
    (a, b, c) * d;

    if !whee {
        todo["whee", 'w'];
        foo("kk", {
            foo
        }).push(("hihi"))[haha]
    } else {
        whee;
    }

    if wazoo {
        return
    } else {
        foo(return "haha" + "hihi", return hehe);
    }

    {
        foo
    }
    a + b
};
