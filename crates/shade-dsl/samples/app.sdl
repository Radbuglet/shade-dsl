const u32 = mod {};

const identity = fn<T: u32>(v: T) -> T {
    v
};

const should_reject = fn(a: u32, b: a) -> a {
    b
};

const add = fn(lhs: u32, rhs: u32) -> u32 {
    identity(lhs) + identity(rhs)
};
